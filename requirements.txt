# Development and validation tools. Runtime dependencies are declared only in
# pyproject.toml and are installed by the editable install used in CI.
build>=1.2
pre-commit>=3.0
types-requests>=2.33.0
types-click>=7.1.0
