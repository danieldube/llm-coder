# Keep the populated copy outside Git in the XDG config directory.
# Create it with mode 0600 before llm-install; see README.md.
# REPLACE_ME is intentionally rejected by validation.
RUNPOD_API_KEY=REPLACE_ME
